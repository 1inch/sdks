# SonarQube Cloud automatic-analysis configuration.
#
# The per-chain contract address tables map every supported chain to the same
# universal deployment address, so each new chain row is a near-copy of the
# previous one by design — copy-paste detection is meaningless for these files.
sonar.cpd.exclusions=typescript/aqua/src/aqua-protocol-contract/constants.ts,typescript/swap-vm/src/swap-vm-contract/constants.ts
